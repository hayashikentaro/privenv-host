{
  "name": "privenv-host",
  "version": "0.1.0",
  "description": "Host-side runtime skeleton for privenv.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "privenv-host": "./dist/src/cli/index.js"
  },
  "scripts": {
    "prebuild": "node scripts/clean-dist.cjs",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/tests/**/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "npm run build --silent && node dist/src/cli/index.js",
    "pack:check": "npm run build && npm test && npm pack --json --cache .npm-cache | node dist/scripts/check-package-files.js"
  },
  "dependencies": {
    "@privenv/protocol": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist/src/",
    "dist/scripts/",
    "README.md",
    "LICENSE",
    "docs/"
  ]
}
